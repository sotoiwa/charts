apiVersion: v1
kind: Service
metadata:
  name: {{ include "subsystem" . }}-np
  labels:
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "subsystem" . }}
  ports:
  - port: 9080
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "subsystem" . }}
  labels:
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: {{ include "subsystem" . }}
  ports:
  - port: 9080
